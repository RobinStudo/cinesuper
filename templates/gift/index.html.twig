{% extends 'layout.html.twig' %}

{% block title %} Mes cadeaux - {{ parent() }}{% endblock %}

{% block content %}

<div class="container">
    <p class="mt-3 h4">
        Vous avez {{ app.user.card.fidelity }} crédit(s) à utiliser dans la boutique.<br>
        N'hésitez pas à vous faire plaisir !!!
    </p>
    <div class="row mt-3 justify-content-center">
        {% for gift in gifts %}
        <div class="card mx-3 mb-4 align-items-center" style="width: 152px">

            <div class="card-body">
                <img src="{{ asset('gifts/' ~  gift.picture) }}" class="card-img-top" style="width: 150px; height:150px" alt="{{ gift.title }}">
                <div class="text-center">
                    <h5 class="card-title">{{gift.title }}</h5>
                    <p class="card-text">Crédit : {{ gift.nbFidelity }}</p>
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ path('buy', {'id': gift.id}) }}" class="btn btn-primary btn-sm">Achetez</a>
            </div>
        </div>
        {% endfor %}
    </div>

    <div class="row mt-4">
        <div class="col">
            <p class="text-center h5">
                Vous souhaitez avoir des crédits ? Pour cela il vous faut prendre des places de cinémas !!!
            </p>
        </div>
    </div>
</div>


{% endblock %}
