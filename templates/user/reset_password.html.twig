{% extends 'layout.html.twig' %}

{% block title %}Réintialisation du mot de passe - {{ parent() }}{% endblock %}

{% block content %}
<div class="container">
    ​<div class="row">
        <div class="col">
            <p class="h1 mb-4">Réinitialiser votre mot de passe</p>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Réintialiser votre mot de passe</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}